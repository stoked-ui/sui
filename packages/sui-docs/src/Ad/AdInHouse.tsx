import * as React from 'react';

interface AdInHouseProps {
  ad: Record<string, any>; // Adjust the type based on the actual structure of `ad`
}

export default function AdInHouse({ ad }: AdInHouseProps) {
  return <React.Fragment />;
}
