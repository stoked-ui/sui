/**
 * Exports a named ID and all its exports.
 * 
 * @description Imports the `import` module using the `namedId` function, which returns a named ID and all its exports.
 * @returns {any} The named ID and all its exports.
 */
export default import.namedId;

export * from './namedId';