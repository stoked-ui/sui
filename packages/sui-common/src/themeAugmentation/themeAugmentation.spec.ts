/*
import {createTheme} from '@mui/material/styles';
import {commonClasses} from '../Common';
import {commonActionClasses} from '../CommonAction';

createTheme({
  components: {
    MuiCommon: {
      defaultProps: {
      },
      styleOverrides: {
        root: {
          backgroundColor: 'red',
          [`.${commonClasses.root}`]: {
            backgroundColor: 'green',
          },
        },
      },
    },
  },
});
*/
