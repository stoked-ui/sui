/**
 * TimelineTrackArea class, which extends the base component.
 *
 * @description This module exports the TimelineTrackArea class.
 */

import TimelineTrackArea from './TimelineTrackArea';

/**
 * Default export of the module.
 */
export default TimelineTrackArea;

/**
 * Export all classes from the timelineTrackAreaClasses module.
 *
 * @see {@link timelineTrackAreaClasses}
 */
export * from './timelineTrackAreaClasses';

/**
 * Export all types from the TimelineTrackArea.types module.
 *
 * @see {@link TimelineTrackArea.types}
 */
export * from './TimelineTrackArea.types';