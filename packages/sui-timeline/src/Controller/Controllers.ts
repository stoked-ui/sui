/**
 * Collection of controllers stored as key-value pairs.
 * @typedef {Record<string, IController>} Controllers
 */

import { IController } from "./Controller.types";

/** 
 * Collection of controllers stored as key-value pairs.
 * @type {Controllers}
 */
const Controllers: Record<string, IController> = {};
export default Controllers;