---
axiom_version: 1
scope: directory
target: packages/sui-file-explorer
status: active
---

# AXIOMS

## AX-MOD-FILE-EXPLORER-001: Public barrel defines the contract

The set of symbols re-exported by `src/index.ts` (components, hooks, models,
plugins, icons, theme augmentation, feature flags) is the public contract of
`@stoked-ui/file-explorer`. Removing or renaming any export is a breaking
change that requires a governed task with explicit consumer review (editor,
docs demos, showcase cards).

### Applies To
- `packages/sui-file-explorer/src/index.ts`
- `packages/sui-file-explorer/src/internals/index.ts`

### Acceptance Checks
- `pnpm --filter @stoked-ui/file-explorer typescript`
- manual: confirm every removed/renamed symbol has been searched repo-wide (`docs/`, `packages/sui-editor`) and updated.

### Enforcement
suite

## AX-MOD-FILE-EXPLORER-002: Plugin order in FILE_EXPLORER_PLUGINS is load-bearing

The tuple `FILE_EXPLORER_PLUGINS` in `src/FileExplorer/FileExplorer.plugins.ts`
declares plugin execution order: `useFileExplorerFiles` →
`useFileExplorerExpansion` → `useFileExplorerSelection` →
`useFileExplorerFocus` → `useFileExplorerKeyboardNavigation` →
`useFileExplorerIcons` → `useFileExplorerGrid` → `useFileExplorerDnd`.
Later plugins read state set by earlier ones. Reordering or inserting plugins
in the middle of this list MUST be accompanied by a full plugin-test run.

### Applies To
- `packages/sui-file-explorer/src/FileExplorer/FileExplorer.plugins.ts`
- `packages/sui-file-explorer/src/internals/plugins/**`

### Acceptance Checks
- `pnpm --filter @stoked-ui/file-explorer typescript`
- manual: run the `useFileExplorer*` plugin test suites under the root mocha command (note: see SC_TEST.md §1.2 — the harness import paths currently need fixing before tests run cleanly).

### Enforcement
suite

## AX-MOD-FILE-EXPLORER-003: File validation is the security gate for external drops

`src/internals/plugins/useFileExplorerDnd/fileValidation.ts`
(`isDangerousExtension`, `isAllowedMimeType`, `isAcceptableFileSize`,
`validateFile`, `validateFiles`, `getRejectionReason`) is the only sanctioned
boundary between OS-originated drops and consumer code. Loosening the default
denylist/allowlist, raising default size caps, or bypassing `validateFiles`
on the external-drop path requires a security review.

### Applies To
- `packages/sui-file-explorer/src/internals/plugins/useFileExplorerDnd/fileValidation.ts`
- `packages/sui-file-explorer/src/internals/plugins/useFileExplorerDnd/useFileExplorerDnd.tsx`
- `packages/sui-file-explorer/src/FileDropzone/**`

### Acceptance Checks
- manual: confirm every external-drop code path (`monitorForExternal`, `dropTargetForExternal`, `FileDropzone` `onDrop`) routes incoming `File` objects through `validateFile`/`validateFiles` before invoking consumer callbacks.
- manual: audit any added entry to allowed MIME types or extension lists for executable/script formats.
- `grep -q "isDangerousExtension" packages/sui-file-explorer/src/internals/plugins/useFileExplorerDnd/fileValidation.ts` (the denylist gate exists).

### Enforcement
computational

## AX-MOD-FILE-EXPLORER-004: FileBase shape is a cross-package data contract

`FileBase` / `FileBaseInput` / `FileId` in `src/models/items.ts` are consumed
by `@stoked-ui/editor`, the docs demos under `docs/data/file-explorer/**`,
and showcase cards. Required-field additions, type-narrowing changes, or
`mediaType` enum bumps (which originate in `@stoked-ui/media`) ripple
repo-wide; such changes require a coordinated migration.

### Applies To
- `packages/sui-file-explorer/src/models/items.ts`

### Acceptance Checks
- `pnpm --filter @stoked-ui/file-explorer typescript`
- manual: run `pnpm typescript` at the monorepo root to catch consumer breakage in `packages/sui-editor` and `docs/`.

### Enforcement
suite

## AX-MOD-FILE-EXPLORER-005: Theme augmentation must mirror new public slots and classes

Adding a `MuiFileExplorer*`, `MuiFile`, or `MuiFileExplorerTabs` slot or
classKey requires a matching entry in `src/themeAugmentation/`. Without it,
consumer themes lose type safety on the new surface.

### Applies To
- `packages/sui-file-explorer/src/themeAugmentation/**`
- `packages/sui-file-explorer/src/**/fileExplorer*Classes.ts`
- `packages/sui-file-explorer/src/**/fileClasses.ts`

### Acceptance Checks
- `pnpm --filter @stoked-ui/file-explorer typescript`
- manual: verify `themeAugmentation.spec.ts` covers the new slot/class.

### Enforcement
suite

## AX-MOD-FILE-EXPLORER-006: Existing tests must keep passing before mutation

Tests under `packages/sui-file-explorer/src/**/*.test.{ts,tsx}` and
`themeAugmentation.spec.ts` define the observable behavior contract of this
module. They must continue to pass (with the harness-path fix from
SC_TEST.md §1.2 applied) before any mutation to this module is merged.

### Applies To
- `packages/sui-file-explorer/src/**/*.test.{ts,tsx}`
- `packages/sui-file-explorer/src/themeAugmentation/themeAugmentation.spec.ts`

### Acceptance Checks
- `pnpm test -- 'packages/sui-file-explorer/src/**/*.test.{ts,tsx}'`
- manual: if a test is intentionally changed, update SC_TEST.md to reflect the new expectation.

### Enforcement
suite

## AX-MOD-FILE-EXPLORER-007: FileExplorerWithFlags must preserve the dual-rendering kill switch and flag dependency graph

`FileExplorerWithFlags` (`src/FileExplorer/FileExplorerWithFlags.tsx`) MUST keep
a working fallback to `FileExplorerLegacy` whenever
`FeatureFlag.USE_MUI_X_RENDERING` is off, and the DnD subsystem flags
(`DND_INTERNAL`, `DND_EXTERNAL`, `DND_TRASH`) MUST remain gated by their
`FEATURE_FLAG_DEPENDENCIES` entries (all three currently require
`USE_MUI_X_RENDERING`). This dual-rendering switch is the production rollout /
emergency-disable kill switch for the MUI-X migration; removing the legacy
fallback path, defaulting `USE_MUI_X_RENDERING` on without a fallback, or
breaking the dependency graph is a governed change.

### Applies To
- `packages/sui-file-explorer/src/FileExplorer/FileExplorerWithFlags.tsx`
- `packages/sui-file-explorer/src/FileExplorer/FileExplorerLegacy.tsx`
- `packages/sui-file-explorer/src/featureFlags/FeatureFlagConfig.ts`
- `packages/sui-file-explorer/src/featureFlags/FeatureFlagContext.tsx`

### Acceptance Checks
- `pnpm --filter @stoked-ui/file-explorer typescript`
- manual: with `USE_MUI_X_RENDERING=false`, `FileExplorerWithFlags` renders `FileExplorerLegacy` (the `if (!useMuiXRendering) return <FileExplorerLegacy …/>` branch must remain).
- manual: every DnD flag in `FEATURE_FLAG_DEPENDENCIES` still lists `USE_MUI_X_RENDERING` (directly or transitively) as a prerequisite.
- run the `featureFlags/FeatureFlagConfig.test.ts` and `featureFlags/FeatureFlagContext.test.tsx` suites.
- `grep -q "if (!useMuiXRendering)" packages/sui-file-explorer/src/FileExplorer/FileExplorerWithFlags.tsx` (the legacy-fallback branch must remain).

### Enforcement
computational

## AX-MOD-FILE-EXPLORER-008: Mocha is the only test runner — no package-local Jest/Vitest config

Tests in this package run exclusively under the repo-root Mocha runner
(root `.mocharc.js`, Chai assertions, Sinon spies, JSDOM via
`@stoked-ui/internal-test-utils`). Adding a package-local `jest.config.*` or
`vitest.config.*` (or new test files importing `@jest/globals` / `vitest`)
fragments the single-runner convention documented in SC_TEST.md §2 and is
not permitted; the one existing Jest-style file
(`src/internals/plugins/useFileExplorerDnd/fileExportUtils.test.ts`) is a
known anomaly to be converted to Chai/Mocha, not a precedent.

### Applies To
- `packages/sui-file-explorer/jest.config.*`
- `packages/sui-file-explorer/vitest.config.*`
- `packages/sui-file-explorer/src/**/*.test.{ts,tsx,js}`

### Acceptance Checks
- `test -z "$(find packages/sui-file-explorer -maxdepth 1 \( -name 'jest.config.*' -o -name 'vitest.config.*' \) 2>/dev/null)"` (no package-local runner config exists).
- `test "$(grep -rl "@jest/globals" packages/sui-file-explorer/src | grep -v fileExportUtils.test.ts | wc -l)" -eq 0` (no new Jest-style test files beyond the known anomaly).

### Enforcement
computational

<!-- stokd-axiom-candidate
rationale: "All test files must load under the root Mocha glob" cannot yet be
an active axiom — as of 2026-07-02, ~12 test files import shared-harness paths
that do not exist (`test/utils/fileExplorer-view/describeFileExplorer`,
`test/utils/tree-view/…`, `test/utils/file-list/…`; the harness actually lives
at `test/utils/file-explorer/describeFileExplorer`), `File.test.tsx` targets
upstream `@mui/x-file-list` instead of the local `src/File`, and
`FileDropzone.test.js` duplicates `FileDropzone.test.tsx`. Promote to an
active axiom ("every `*.test.{ts,tsx}` file loads without MODULE_NOT_FOUND
under `cross-env NODE_ENV=test mocha`") once the SC_TEST.md §6 migration
lands; until then the invariant is violated and unsatisfiable.
-->

<!-- stokd-axiom-candidate
rationale: DEFAULT_FEATURE_FLAGS (src/featureFlags/FeatureFlagConfig.ts:104)
currently enables all four flags at 100% traffic, and the production entry in
ENVIRONMENT_CONFIGS deliberately overrides nothing so runtime overrides are
the rollout lever. An axiom pinning "production flag posture changes only via
a governed task" may be warranted, but the intended long-term defaults are
not yet settled (MUI-X RichTreeView integration is still pending — both
rendering paths currently delegate to the legacy renderer), so this stays a
candidate rather than freezing today's temporary posture as law.
-->

<!-- stokd-axiom-candidate
suggested_targets: .stokd/meta/SC_AXIOMS.md
rationale: Every Stoked UI package in this monorepo (sui-common, sui-editor,
sui-file-explorer, sui-media, sui-timeline, …) builds with the same modern +
node + stable + types pipeline and exposes `src/index.ts` as the source entry.
A repo-wide axiom that "publishable @stoked-ui/* packages must keep
`pnpm build` (all four targets) green before merge" would belong at the
repo-global level rather than each package's `.axioms.md`.
-->

<!-- stokd-axiom-candidate
suggested_targets: .stokd/meta/SC_AXIOMS.md
rationale: Multiple packages re-export types from `@stoked-ui/media`
(`MediaType`, `MediaFile`). A repo-global axiom that "bumps to
@stoked-ui/media's enum / type surface require coordinated PRs across all
peer consumers" applies beyond just this module.
-->
