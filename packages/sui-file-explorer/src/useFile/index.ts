/**
 * Exported items from useFile module.
 */
export { useFile } from './useFile';

/**
 * Types exported from useFile module.
 * @typedef {Object} UseFileParameters - Parameters for the useFile hook.
 * @typedef {Object} UseFileReturnValue - Return value of the useFile hook.
 * @typedef {Object} UseFileContentSlotOwnProps - Own props for the content slot in useFile.
 */
export type {
  UseFileParameters,
  UseFileReturnValue,
  UseFileContentSlotOwnProps,
} from './useFile.types';
