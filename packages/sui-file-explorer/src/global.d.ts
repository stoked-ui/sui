/**
 * Declares a global interface for FileBase.
 */
declare global {
  interface FileBase {
    /**
     * Optional property to specify the visible index.
     */
    visibleIndex?: number;
  }
}

export {};