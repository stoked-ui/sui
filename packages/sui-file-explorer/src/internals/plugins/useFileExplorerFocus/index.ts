/**
 * Exports the `useFileExplorerFocus` hook from the `./useFileExplorerFocus` file.
 */
export { useFileExplorerFocus } from './useFileExplorerFocus';

/**
 * Type definitions for the `useFileExplorerFocus` hook.
 * 
 * @interface UseFileExplorerFocusSignature
 * @see ./useFileExplorerFocus
 */
export type {
  /**
   * Signature of the `useFileExplorerFocus` hook.
   * 
   * @param {Object} params - Parameters to pass to the hook.
   * @returns {Object} - Return value of the hook.
   */
  UseFileExplorerFocusSignature,
  /**
   * Defaultized parameters for the `useFileExplorerFocus` hook.
   * 
   * @see UseFileExplorerFocusParameters
   */
  UseFileExplorerFocusDefaultizedParameters,
} from './useFileExplorerFocus.types';

/**
 * Type definitions for the parameters of the `useFileExplorerFocus` hook.
 * 
 * @interface UseFileExplorerFocusParameters
 * @see ./useFileExplorerFocus.types
 */
export type {
  /**
   * Parameters to pass to the `useFileExplorerFocus` hook.
   */
  UseFileExplorerFocusParameters,
} from './useFileExplorerFocus.types';