/**
 * Exports the FileDepthContext class.
 */
export { 
  /**
   * A context for file depth calculations.
   *
   * @example
   * // Import the FileDepthContext
   * import { FileDepthContext } from './FileDepthContext';
   * 
   * // Create a new instance of the context
   * const context = new FileDepthContext();
   */
  FileDepthContext 
}