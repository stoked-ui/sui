/**
 * @module useTimeout
 *
 * @description A utility hook for setting a timeout.
 *
 * @example
 * import { useTimeout } from '@mui/utils/useTimeout';
 * 
 * const [timeout, clearTimeout] = useTimeout(1000);
 */

export { default as useTimeout } from '@mui/utils/useTimeout';