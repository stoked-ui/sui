/**
 * A custom hook that executes a callback function on component mount.
 * 
 * @typedef {import('@mui/utils/useOnMount').useOnMount} useOnMount
 * 
 * @returns {useOnMount} The callback function to be executed on component mount.
 */
export { default as useOnMount } from '@mui/utils/useOnMount';