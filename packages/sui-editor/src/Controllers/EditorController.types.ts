/*
import {
  IController,
} from "@stoked-ui/timeline";
import { IEditorAction } from "../EditorAction/EditorAction";
import { EditorEventTypes, IEditorEngine, IEditorTrack } from "../EditorEngine";

export interface IEditorController<
  ActionType extends IEditorAction = IEditorAction,
  EngineType extends IEditorEngine = IEditorEngine,
> extends IController<ActionType, EngineType> { }

*/
