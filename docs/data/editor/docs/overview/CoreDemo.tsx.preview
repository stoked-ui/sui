export default function CoreDemo() {
  const [data, setData] = React.useState(defaultEditorData);

  return (
    <Editor id='video-editor' labels sx={{ borderRadius: '12px 12px 0 0' }} actionData={data} />
  );
};

